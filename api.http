# LAM-Audio2Expression API（仅保留已验证可用的接口）
# 基础路径：所有路由都在 `gradio_api` 命名空间下；旧版 `/api/...` 会 404。
# 注意：前端必须访问后端端口（默认 7860），不要打到 5173。

@host = http://127.0.0.1:7860
@image = assets/sample_input/james.png
@audio = assets/sample_audio/BarackObama_english.wav

### 获取端点信息（含 named_endpoints）
GET {{host}}/gradio_api/info

### 异步调用（已验证）：返回 event_id
# 说明：当前版本未开放可用的 SSE 结果订阅接口，请不要尝试订阅流。
#      本接口用于确认后端已收到并排队任务。
POST {{host}}/gradio_api/call/a2e_predict
Content-Type: application/json

{
  "data": [
    {"path": "{{image}}"},
    {"path": "{{audio}}"},
    ""
  ]
}

### 文件下载（已验证）：静态文件
# 注意：返回路径需要拼接 host，例如：
#   {{host}}/gradio_api/file=assets/sample_audio/BarackObama_english.wav
GET {{host}}/gradio_api/file=assets/sample_audio/BarackObama_english.wav

### 已知限制
# - /gradio_api/run/a2e_predict：当前环境调用返回 500（内部错误），暂不作为可用接口写入文档。
# - /gradio_api/stream/...：当前环境返回 404/405，未开放可用 SSE 流；不要在前端订阅。
# - 若需完整“调用→结果下载”流程，请先修复后端推理错误（见终端 numba/pydantic 报错）。

